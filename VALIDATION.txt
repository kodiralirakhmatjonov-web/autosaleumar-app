AUTO SALE UMAR — NATIVE SWIFTUI CUMULATIVE PHASE VI / XCODE COMPILE REPAIR 2

PURPOSE
- One cumulative mobile-update.zip for the current Auto Sale Umar native iOS repository.
- Includes the full Phase I–VI native SwiftUI client payload and all compile repairs from supplied GitHub logs.
- Website repository remains untouched.

IMPORTANT WORKFLOW FACT
- In run 92006087015, the ZIP extraction/apply step DID succeed in the GitHub runner workspace: "Native iOS update applied in workspace."
- The workflow failed later at the Xcode compile gate (exit code 65), before commit/push.
- Therefore those extracted files did not enter main. This is intentional protection in the workflow.
- Rerunning the old failed job would use the same old commit/archive and would not apply this repaired ZIP.

PREVIOUS REPAIRS RETAINED
1) HomeComponents.swift Theme Picker:
   Text($0.title(settings.language))
2) ProfileView.swift customer profile save:
   local trimmingCharacters(...) instead of cross-file private String.trimmed.

RUN 92006087015 — ACTUAL XCODE ERRORS FIXED
- Sources/Views/ClientForms.swift:291:35 — RequestCarView had no member showroomCalendar.
- Sources/Views/ClientForms.swift:292:35 — RequestCarView had no member showroomCalendar.
- Sources/Views/ClientForms.swift:559:22 — showroomDatePart not found in BookingView scope.
- Sources/Views/ClientForms.swift:560:22 — showroomDatePart not found in BookingView scope.
- Sources/Views/ClientForms.swift:561:22 — showroomDatePart not found in BookingView scope.

ROOT CAUSE
- showroomDatePart had accidentally been placed inside RequestCarView while showroomCalendar/dateButton belong to BookingView.

FIX
- Moved showroomDatePart into BookingView, beside nextDates/dateButton.
- Self.showroomCalendar now resolves to BookingView.showroomCalendar in the correct lexical scope.

VALIDATION PERFORMED ON THIS PAYLOAD
- Swift syntax parse: PASS (33/33 Swift files).
- Exact HomeComponents compiler regression scan: PASS.
- Exact ProfileView private-access regression scan: PASS.
- Exact ClientForms BookingView scope regression: PASS.
- All supplied run 92006087015 compiler diagnostics are directly addressed.
- Backend/worker.js node --check: PASS.
- Asset catalog Contents.json parse: PASS.
- Support/Info.plist parse: PASS.
- PrivacyInfo.xcprivacy parse: PASS.
- project.yml YAML parse: PASS.
- ZIP integrity/path traversal validation: PASS.
- Cumulative DELETE.txt safe-apply simulation: PASS.
- No .github payload.
- No Signing payload.

LIMITATION
A full Xcode 26 compile is only available in the GitHub macOS runner, so the repository workflow remains the authoritative semantic/build gate.
