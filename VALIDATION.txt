AUTO SALE UMAR APP — R2 MEDIA PROXY UPDATE

This update changes only the app repository backend worker.
The website repository is untouched.
The iOS UI/source is untouched.
The app API now returns /v1/media/:id image URLs backed by existing car_media records and existing R2 buckets.
